[project]
name = "rf-voice-to-blog"
version = "0.1.0"

dependencies = [
  "PyAudio",
  "vosk",
  "cffi",
  "gpt4all",
  "pydub",
  "SpeechRecognition",
  "deepgram-sdk",
  "pyyaml",
  # "transformers",
  # "torch",
]

[tool.setuptools.packages.find]
include = [
  "rf-voice-to-blog",
]
